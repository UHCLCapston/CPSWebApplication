@model CPSWebApplication.Models.ViewModel.DesignCPSViewModel

@{
    ViewBag.Title = "StudentCPSDesign";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>StudentCPSDesign</h2>

<h4>@Model.lastName</h4>
@using (Html.BeginForm())
{
    <div>
        <div><h6>@Model.majorName</h6>
        <h6>@Model.academicYear</h6>
        </div>
        <br /><br />
        <div>
           
        </div>
        <br /><br />
        <div>
            <button type="submit" class="btn btn-default" onclick="window.location = ('@Url.Action("","")')">Save</button>
        </div>
    </div>
}